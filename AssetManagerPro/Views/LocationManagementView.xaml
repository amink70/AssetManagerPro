<UserControl x:Class="AssetManagerPro.Views.LocationManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">

            <Button x:Name="btnAdd"
                    Width="120"
                    Height="40"
                    Margin="0,0,10,0"
                    Content="➕ افزودن"
                    Click="btnAdd_Click"/>

            <Button x:Name="btnDelete"
                    Width="120"
                    Height="40"
                    Content="🗑 حذف"
                    Click="btnDelete_Click"/>

        </StackPanel>

        <DataGrid x:Name="dgLocations"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  MouseDoubleClick="dgLocations_MouseDoubleClick">

            <DataGrid.Columns>

                <DataGridTextColumn Header="شناسه"
                                    Binding="{Binding Id}"
                                    Width="80"/>

                <DataGridTextColumn Header="نام مکان"
                                    Binding="{Binding Name}"
                                    Width="220"/>

                <DataGridTextColumn Header="دپارتمان"
                    Binding="{Binding DepartmentName}"
                    Width="180"/>

                <DataGridTextColumn Header="توضیحات"
                                    Binding="{Binding Description}"
                                    Width="*"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>