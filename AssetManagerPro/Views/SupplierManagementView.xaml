<UserControl x:Class="AssetManagerPro.Views.SupplierManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">

            <Button x:Name="btnAdd"
                    Click="btnAdd_Click"
                    Width="120"
                    Height="40"
                    Margin="0,0,10,0"
                    Content="➕ افزودن"/>

            <Button x:Name="btnDelete"
                    Click="btnDelete_Click"
                    Width="120"
                    Height="40"
                    Content="🗑 حذف"/>

        </StackPanel>

        <DataGrid x:Name="dgSuppliers"
                  MouseDoubleClick="dgSuppliers_MouseDoubleClick"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">

            <DataGrid.Columns>

                <DataGridTextColumn Header="شناسه"
                                    Binding="{Binding Id}"
                                    Width="80"/>

                <DataGridTextColumn Header="نام تأمین‌کننده"
                                    Binding="{Binding Name}"
                                    Width="220"/>

                <DataGridTextColumn Header="مدیر"
                                    Binding="{Binding ManagerName}"
                                    Width="180"/>

                <DataGridTextColumn Header="تلفن"
                                    Binding="{Binding Phone}"
                                    Width="150"/>

                <DataGridTextColumn Header="ایمیل"
                                    Binding="{Binding Email}"
                                    Width="220"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>