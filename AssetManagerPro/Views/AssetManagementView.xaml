<UserControl x:Class="AssetManagerPro.Views.AssetManagementView"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F3F4F6">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- عنوان -->
        <TextBlock Text="مدیریت اموال"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- نوار ابزار -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,20">

            <Button Content="افزودن"
        Width="120"
        Style="{StaticResource ModernButton}"
        Click="btnAdd_Click"/>
            <Button Content="ویرایش"
        Width="120"
        Margin="10,0,0,0"
        Style="{StaticResource ModernButton}"
        Click="btnEdit_Click"/>

            <Button Content="حذف"
        Width="120"
        Margin="10,0,0,0"
        Style="{StaticResource ModernButton}"
        Click="btnDelete_Click"/>
            <Button Content="📜 تاریخچه"
        Width="120"
        Margin="10,0,0,0"
        Style="{StaticResource ModernButton}"
        Click="btnHistory_Click"/>

            <TextBox x:Name="txtSearch"
         Style="{StaticResource SearchTextBox}"
         Width="250"
         Margin="30,0,0,0"
         TextChanged="txtSearch_TextChanged"/>
        </StackPanel>

        <!-- جدول -->

        <DataGrid x:Name="dgAssets"
                  Grid.Row="2"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          IsReadOnly="True"
          HeadersVisibility="Column"
          ItemsSource="{Binding Assets}" >

            <DataGrid.Columns>

                <DataGridTextColumn Header="کد اموال"
                        Binding="{Binding AssetCode}"
                        Width="110"/>

                <DataGridTextColumn Header="نام کالا"
                        Binding="{Binding Name}"
                        Width="180"/>

                <DataGridTextColumn Header="برند"
                        Binding="{Binding Brand}"
                        Width="120"/>

                <DataGridTextColumn Header="دسته‌بندی"
                        Binding="{Binding Category}"
                        Width="130"/>

                <DataGridTextColumn Header="مدل"
                        Binding="{Binding Model}"
                        Width="120"/>

                <DataGridTextColumn Header="شماره سریال"
                        Binding="{Binding SerialNumber}"
                        Width="170"/>

                <DataGridTextColumn Header="محل استقرار"
                        Binding="{Binding Location}"
                        Width="150"/>

                <DataGridTextColumn Header="تحویل‌گیرنده"
                        Binding="{Binding Receiver}"
                        Width="160"/>

                <DataGridTextColumn Header="وضعیت"
                        Binding="{Binding Status}"
                        Width="110"/>

                <DataGridTextColumn Header="قیمت"
                        Binding="{Binding Price,StringFormat={}{0:N0}}"
                        Width="120"/>

                <DataGridTextColumn Header="تاریخ خرید"
                        Binding="{Binding PurchaseDate,StringFormat=yyyy/MM/dd}"
                        Width="130"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>