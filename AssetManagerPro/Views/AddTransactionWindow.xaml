<Window x:Class="AssetManagerPro.Views.AddTransactionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ثبت گردش اموال"
        Height="500"
        Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
               Text="ثبت گردش اموال"
               FontSize="22"
               FontWeight="Bold"
               Margin="0,0,0,20"/>

        <ComboBox x:Name="cmbAssets"
              Grid.Row="1"
              Height="35"
              Margin="0,0,0,10"/>

        <ComboBox x:Name="cmbTransactionType"
              Grid.Row="2"
              Height="35"
              Margin="0,0,0,10"/>

        <ComboBox x:Name="cmbReceivers"
              Grid.Row="3"
              Height="35"
              Margin="0,0,0,10"/>

        <ComboBox x:Name="cmbLocations"
              Grid.Row="4"
              Height="35"
              Margin="0,0,0,10"/>

        <DatePicker x:Name="dpTransactionDate"
                Grid.Row="5"
                Height="35"
                Margin="0,0,0,10"/>

        <TextBox x:Name="txtDescription"
             Grid.Row="6"
             AcceptsReturn="True"
             TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="7"
                Orientation="Horizontal"
                HorizontalAlignment="Left"
                Margin="0,15,0,0">

            <Button Click="btnSave_Click" 
                x:Name="btnSave"
                Width="120"
                Height="40"
                Margin="0,0,10,0"
                Style="{StaticResource ModernButton}"
                Content="💾 ثبت"/>

            <Button 
                Click="btnCancel_Click"
                x:Name="btnCancel"
                Width="120"
                Height="40"
                Style="{StaticResource ModernButton}"
                Content="❌ انصراف"/>

        </StackPanel>

    </Grid>

</Window>