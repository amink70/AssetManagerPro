<UserControl x:Class="AssetManagerPro.Views.ReceiverManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">

            <Button x:Name="btnAdd"
                    Width="120"
                    Height="40"
                    Margin="0,0,10,0"
                    Content="➕ افزودن"
                    Click="btnAdd_Click"/>

            <Button x:Name="btnDelete"
                    Width="120"
                    Height="40"
                    Content="🗑 حذف"
                    Click="btnDelete_Click"/>

        </StackPanel>

        <DataGrid x:Name="dgReceivers"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  MouseDoubleClick="dgReceivers_MouseDoubleClick">

            <DataGrid.Columns>

                <DataGridTextColumn Header="شناسه"
                                    Binding="{Binding Id}"
                                    Width="70"/>

                <DataGridTextColumn Header="نام و نام خانوادگی"
                                    Binding="{Binding FullName}"
                                    Width="220"/>

                <DataGridTextColumn Header="کد پرسنلی"
                                    Binding="{Binding PersonnelCode}"
                                    Width="130"/>

                <DataGridTextColumn Header="دپارتمان"
                                    Binding="{Binding DepartmentName}"
                                    Width="170"/>

                <DataGridTextColumn Header="تلفن"
                                    Binding="{Binding Phone}"
                                    Width="140"/>

                <DataGridTextColumn Header="ایمیل"
                                    Binding="{Binding Email}"
                                    Width="220"/>

                <DataGridCheckBoxColumn Header="فعال"
                                        Binding="{Binding IsActive}"
                                        Width="80"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>